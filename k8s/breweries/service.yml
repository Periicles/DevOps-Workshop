apiVersion: v1
kind: Service
metadata:
  name: breweries-service
  namespace: default
  labels:
    app: breweries-service
spec:
  type: ClusterIP
  selector:
    app: breweries-service
  ports:
  - port: 3001
    targetPort: 3001
    protocol: TCP
    name: http
